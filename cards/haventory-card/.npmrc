# Commit the lockfile for reproducible `npm ci` installs. The card is built and
# tested on Linux CI; modern npm records platform-conditional optional deps in
# the lock, so it resolves correctly on any dev OS.
package-lock=true
